# Full NLPTrader requirements
fastapi>=0.110
uvicorn>=0.29
requests>=2.31
python-dotenv>=1.0
feedparser>=6.0
openai>=1.30

# For FinBERT (local pipeline):
#   pip install transformers torch
# The pipeline scripts check for torch before running.
