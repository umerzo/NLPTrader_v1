"""
NLPTrader is now served via FastAPI + HTML frontend.
Run: uvicorn api:app --reload   then open http://localhost:8000

Streamlit frontend has been removed — only the HTML/FastAPI frontend is maintained.
"""
